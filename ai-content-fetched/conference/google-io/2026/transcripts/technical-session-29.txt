kind: captions language: en [music] &gt;&gt; hello, i'm ben, a developer relations &gt;&gt; hello, i'm ben, a developer relations &gt;&gt; hello, i'm ben, a developer relations engineer working on navigation 3. and engineer working on navigation 3. and i'll be joined by clara. i'll be joined by clara. in this session, we'll be covering in this session, we'll be covering in this session, we'll be covering everything you need to know about everything you need to know about everything you need to know about building adaptive layouts for your app building adaptive layouts for your app building adaptive layouts for your app with navigation 3. after a brief with navigation 3. after a brief refresher on the core concepts of the refresher on the core concepts of the refresher on the core concepts of the library, we'll cover what's new in the library, we'll cover what's new in the library, we'll cover what's new in the 1.1 release. 1.1 release. that includes a deep dive on scene that includes a deep dive on scene that includes a deep dive on scene decorators, which can be used to add decorators, which can be used to add decorators, which can be used to add common ui elements such as top and common ui elements such as top and common ui elements such as top and bottom bars around your app's main bottom bars around your app's main bottom bars around your app's main content. to close things out, we'll take a look to close things out, we'll take a look to close things out, we'll take a look at how navigation 3 is expanding to wear at how navigation 3 is expanding to wear at how navigation 3 is expanding to wear os and give you a preview of what we're os and give you a preview of what we're os and give you a preview of what we're working on for 1.2. hello, i'm clara. first, let's go over hello, i'm clara. first, let's go over some basic navigation 3 concepts. some basic navigation 3 concepts. navigation 3 was built from the ground navigation 3 was built from the ground navigation 3 was built from the ground up with compose state as a fundamental up with compose state as a fundamental up with compose state as a fundamental building block. building block. this means that it fully buys into the this means that it fully buys into the this means that it fully buys into the declarative programming model. declarative programming model. you change the state you own, and we you change the state you own, and we you change the state you own, and we react to that new state. react to that new state. to start, the navigation back stack is to start, the navigation back stack is to start, the navigation back stack is just a regular list that you fully own. just a regular list that you fully own. the back stack contains keys which can the back stack contains keys which can the back stack contains keys which can be of any type. and each key in the back be of any type. and each key in the back stack maps to a nav entry that contains stack maps to a nav entry that contains stack maps to a nav entry that contains a composable content. a composable content. as you add and remove keys on the back as you add and remove keys on the back as you add and remove keys on the back stack, navigation reacts by recomposing stack, navigation reacts by recomposing stack, navigation reacts by recomposing the content based on the current keys on the content based on the current keys on the content based on the current keys on the stack. to set this up, define your nav key and to set this up, define your nav key and to set this up, define your nav key and create a back stack with the key. create a back stack with the key. in this example, the key extends the in this example, the key extends the in this example, the key extends the optional nav key interface. optional nav key interface. then, invoke the nav display composable then, invoke the nav display composable then, invoke the nav display composable and pass in the back stack. and pass in the back stack. now, nav display also needs to know what now, nav display also needs to know what now, nav display also needs to know what content the key maps to. so, you need to content the key maps to. so, you need to pass in an entry provider. pass in an entry provider. the entry provider is responsible for the entry provider is responsible for the entry provider is responsible for providing a nav entry for a given key. providing a nav entry for a given key. in the nav display, call the entry in the nav display, call the entry in the nav display, call the entry provider dsl and add entries with the provider dsl and add entries with the provider dsl and add entries with the nav key and the associated content. and nav key and the associated content. and that's it. nav display will first decide what to nav display will first decide what to nav display will first decide what to display, and then it calls your entry display, and then it calls your entry display, and then it calls your entry provider with the key to get the correct provider with the key to get the correct provider with the key to get the correct entry. in this example, the entry provider will in this example, the entry provider will in this example, the entry provider will return the home nav entry for the home return the home nav entry for the home return the home nav entry for the home key. by default, navigation will render the by default, navigation will render the by default, navigation will render the nav entry associated with the last key nav entry associated with the last key nav entry associated with the last key on the back stack. on the back stack. but what if you want a more complex but what if you want a more complex but what if you want a more complex layout? for example, what if you want to layout? for example, what if you want to display multiple entries at the same display multiple entries at the same display multiple entries at the same time? this is where the scene interface comes this is where the scene interface comes this is where the scene interface comes in. a scene controls which entries are a scene controls which entries are a scene controls which entries are displayed and how they are displayed. displayed and how they are displayed. this means that the same nav display can this means that the same nav display can this means that the same nav display can be used to show entries in different be used to show entries in different be used to show entries in different layouts, such as a dialogue, a layouts, such as a dialogue, a layouts, such as a dialogue, a side-by-side list detail, a horizontal side-by-side list detail, a horizontal side-by-side list detail, a horizontal pager, a bottom sheet, or just a single pager, a bottom sheet, or just a single pager, a bottom sheet, or just a single pane. but how does the nav display pick the but how does the nav display pick the but how does the nav display pick the correct scene to show the entries? correct scene to show the entries? it does that by asking each of the scene it does that by asking each of the scene it does that by asking each of the scene strategy instances you pass to it. strategy instances you pass to it. the nav display goes through a list of the nav display goes through a list of the nav display goes through a list of scene strategies in the order they were scene strategies in the order they were scene strategies in the order they were added to the list. and the first added to the list. and the first strategy that returns a scene dictates strategy that returns a scene dictates strategy that returns a scene dictates the entries to display and the layout to the entries to display and the layout to the entries to display and the layout to display them in. display them in. nav display will then invoke the nav display will then invoke the nav display will then invoke the returned scene to display the actual returned scene to display the actual returned scene to display the actual content. the list of strategies can be modified the list of strategies can be modified the list of strategies can be modified and nav display will just recalculate and nav display will just recalculate and nav display will just recalculate and display the new scene. and display the new scene. now, how does the scene strategy decide now, how does the scene strategy decide now, how does the scene strategy decide whether to return a scene or not? whether to return a scene or not? one of the ways is by evaluating the one of the ways is by evaluating the one of the ways is by evaluating the entries metadata. entries metadata. the nav entry has a metadata field, the nav entry has a metadata field, the nav entry has a metadata field, which is a map of string key to any which is a map of string key to any which is a map of string key to any values. nav entries can opt in to be displayed nav entries can opt in to be displayed nav entries can opt in to be displayed by specific strategies through adding by specific strategies through adding by specific strategies through adding the metadata defined by a strategy. the metadata defined by a strategy. this gives entries control over how it this gives entries control over how it this gives entries control over how it wants to be displayed. wants to be displayed. in this example, the entry opts in to be in this example, the entry opts in to be in this example, the entry opts in to be displayed as a dialogue. displayed as a dialogue. in our 1.1 release, we listened to your in our 1.1 release, we listened to your in our 1.1 release, we listened to your feedback and made several improvements feedback and made several improvements feedback and made several improvements to our apis. to our apis. in the past, scene strategies used to be in the past, scene strategies used to be in the past, scene strategies used to be passed in as a chain of strategies passed in as a chain of strategies passed in as a chain of strategies linked by a then infix. linked by a then infix. however, it was named as a singular however, it was named as a singular however, it was named as a singular scene strategy and the then infix was scene strategy and the then infix was scene strategy and the then infix was not discoverable. not discoverable. so, in 1.1, we renamed the parameter to so, in 1.1, we renamed the parameter to so, in 1.1, we renamed the parameter to a plural scene strategies, and now it a plural scene strategies, and now it a plural scene strategies, and now it takes a list of strategies. takes a list of strategies. but, remember that order still matters but, remember that order still matters but, remember that order still matters here. as mentioned, here. as mentioned, the first strategy that returns a scene the first strategy that returns a scene the first strategy that returns a scene becomes the dictating strategy, and becomes the dictating strategy, and becomes the dictating strategy, and strategies are evaluated by the order strategies are evaluated by the order strategies are evaluated by the order they are added to the list. they are added to the list. next, in 1.1, we also improved the next, in 1.1, we also improved the next, in 1.1, we also improved the metadata apis. metadata apis. as mentioned, the metadata is a map of as mentioned, the metadata is a map of as mentioned, the metadata is a map of string to any, which is not very type string to any, which is not very type string to any, which is not very type safe. also, the keys are commonly declared as also, the keys are commonly declared as also, the keys are commonly declared as string constants, so they are not string constants, so they are not string constants, so they are not readily discoverable. readily discoverable. so, we improved them by adding a nav so, we improved them by adding a nav so, we improved them by adding a nav metadata key interface and a metadata metadata key interface and a metadata metadata key interface and a metadata dsl. now, when you define a metadata key, the now, when you define a metadata key, the now, when you define a metadata key, the key can optionally implement the nav key can optionally implement the nav key can optionally implement the nav metadata key interface. metadata key interface. you'll notice that the interface has a you'll notice that the interface has a you'll notice that the interface has a type parameter. type parameter. the parameter represents the type of the the parameter represents the type of the the parameter represents the type of the data that is associated with the key. data that is associated with the key. this means that when you define the key, this means that when you define the key, this means that when you define the key, you are also declaring the type of data you are also declaring the type of data you are also declaring the type of data it accepts. this helps ensure you store the correct this helps ensure you store the correct this helps ensure you store the correct data type. with a nav metadata key, you can now use with a nav metadata key, you can now use with a nav metadata key, you can now use the metadata dsl. the metadata dsl. the dsl is a type safe map builder that the dsl is a type safe map builder that the dsl is a type safe map builder that provides a put function, which accepts provides a put function, which accepts provides a put function, which accepts keys that extends the new key interface, keys that extends the new key interface, keys that extends the new key interface, and the value associated with the key. and the value associated with the key. the dsl returns a map of string to any the dsl returns a map of string to any the dsl returns a map of string to any containing the data you added with the containing the data you added with the containing the data you added with the put function. put function. and you can use the plus operator to and you can use the plus operator to and you can use the plus operator to combine metadata maps. combine metadata maps. this allows you to declare modular, this allows you to declare modular, this allows you to declare modular, reusable metadatas, which you can mix reusable metadatas, which you can mix reusable metadatas, which you can mix and match to create the final metadata and match to create the final metadata and match to create the final metadata for a given entry. for a given entry. with the dsl, you can also write with the dsl, you can also write with the dsl, you can also write conditional logic to determine the final conditional logic to determine the final conditional logic to determine the final metadata that you want to add. metadata that you want to add. another new feature that was added in another new feature that was added in another new feature that was added in 1.1 is the support for shared nav 1.1 is the support for shared nav 1.1 is the support for shared nav entrie