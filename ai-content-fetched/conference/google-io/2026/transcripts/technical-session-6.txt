kind: captions language: en [music] &gt;&gt; this screen uses the animation trigger &gt;&gt; this screen uses the animation trigger &gt;&gt; this screen uses the animation trigger property that makes scroll trigger property that makes scroll trigger property that makes scroll trigger animations buttery smooth and incredibly animations buttery smooth and incredibly animations buttery smooth and incredibly easy. and this select drop down uses a native and this select drop down uses a native and this select drop down uses a native select element styled with appearance select element styled with appearance select element styled with appearance base select. oh, and this layout uses base select. oh, and this layout uses css grid with non-uniform track sizes, css grid with non-uniform track sizes, css grid with non-uniform track sizes, which tells the user where to look which tells the user where to look which tells the user where to look first, establishing a spatial hierarchy. first, establishing a spatial hierarchy. and we didn't build this with an ai and we didn't build this with an ai and we didn't build this with an ai coding agent. well, it's not entirely coding agent. well, it's not entirely true, right? true, right? well, okay, yeah, but it we did build it well, okay, yeah, but it we did build it well, okay, yeah, but it we did build it with a design agent. with a design agent. this is stitch. it's an ai design tool. you describe what you want your app to you describe what you want your app to you describe what you want your app to look and feel like and it's going to look and feel like and it's going to look and feel like and it's going to generate a design backed by real html generate a design backed by real html generate a design backed by real html and tailwind css. and tailwind css. and the most important part of this and the most important part of this and the most important part of this workflow is that every feature we're workflow is that every feature we're workflow is that every feature we're covering in css maps directly to a covering in css maps directly to a covering in css maps directly to a design principle. grid gives us design principle. grid gives us structural hierarchy, and scroll structural hierarchy, and scroll structural hierarchy, and scroll triggered animations create a sense of triggered animations create a sense of triggered animations create a sense of continuity as the user moves through the continuity as the user moves through the continuity as the user moves through the page. and we know how to direct this design and we know how to direct this design and we know how to direct this design because we use the vocabulary, because we use the vocabulary, because we use the vocabulary, descriptions, and even the syntax of descriptions, and even the syntax of descriptions, and even the syntax of these css features. these css features. i'm dion and i work on stitch. &gt;&gt; and and i'm i'm david and you know, i i &gt;&gt; and and i'm i'm david and you know, i i &gt;&gt; and and i'm i'm david and you know, i i also work on stitch. also work on stitch. and today we're going to teach you how and today we're going to teach you how and today we're going to teach you how to vibe design to vibe design to vibe design with css. so, what is vibe design? it's using your intent to build a it's using your intent to build a it's using your intent to build a design. you describe what you want, the design. you describe what you want, the layout, the feel, the behavior, and the layout, the feel, the behavior, and the layout, the feel, the behavior, and the tool builds it. tool builds it. that's just the baseline, though. what makes it more powerful is when you what makes it more powerful is when you what makes it more powerful is when you use your css vocabulary. use your css vocabulary. then your intent gets specific. instead then your intent gets specific. instead of saying "make it look nice", you say of saying "make it look nice", you say of saying "make it look nice", you say "use a non-uniform grid with a "use a non-uniform grid with a "use a non-uniform grid with a two-column hero span." two-column hero span." two-column hero span." instead of "add some animation", you can instead of "add some animation", you can instead of "add some animation", you can say "bind a scale y transform to the say "bind a scale y transform to the say "bind a scale y transform to the scroll timeline." scroll timeline." scroll timeline." your css knowledge turns vague intent your css knowledge turns vague intent your css knowledge turns vague intent into precise direction. into precise direction. css is the shared language between you, css is the shared language between you, css is the shared language between you, design, code, and the agent. design, code, and the agent. and that's the workflow that we're going and that's the workflow that we're going and that's the workflow that we're going to cover today. we're going to go over to cover today. we're going to go over how to go from a css idea to a finished how to go from a css idea to a finished how to go from a css idea to a finished design using stitch and the chrome design using stitch and the chrome design using stitch and the chrome devtools mcps together. devtools mcps together. then we'll start with layouts. we'll then we'll start with layouts. we'll build a grid with non-uniform track build a grid with non-uniform track build a grid with non-uniform track sizes that showcases visual hierarchy sizes that showcases visual hierarchy sizes that showcases visual hierarchy because like who doesn't love a good because like who doesn't love a good because like who doesn't love a good grid? and after that, we'll cover how to grid? and after that, we'll cover how to define your visual identity and have define your visual identity and have define your visual identity and have every screen inherit from it. colors, every screen inherit from it. colors, typography, spacing, all of it in one typography, spacing, all of it in one typography, spacing, all of it in one source of truth. source of truth. then we'll get into the weeds on the then we'll get into the weeds on the then we'll get into the weeds on the fundamentals of scroll-triggered fundamentals of scroll-triggered fundamentals of scroll-triggered animations to create them right from animations to create them right from animations to create them right from prompts. and last and definitely not least, we'll and last and definitely not least, we'll and last and definitely not least, we'll cover appearance-based selector style a cover appearance-based selector style a cover appearance-based selector style a select element without any javascript or select element without any javascript or select element without any javascript or any libraries. any libraries. and we're going to leave all of the and we're going to leave all of the and we're going to leave all of the prompts, the tools, and everything we prompts, the tools, and everything we prompts, the tools, and everything we did in the description today. did in the description today. so before we start prompting, let's set so before we start prompting, let's set so before we start prompting, let's set up the development environment. we're up the development environment. we're using two mcp servers that work together using two mcp servers that work together using two mcp servers that work together with antigravity as our ide. with antigravity as our ide. first, stitch mcp. this connects your first, stitch mcp. this connects your coding agent to stitch so it can create coding agent to stitch so it can create coding agent to stitch so it can create projects, generate screens, and edit projects, generate screens, and edit projects, generate screens, and edit designs programmatically. designs programmatically. open the antigravity mcp gallery, find open the antigravity mcp gallery, find open the antigravity mcp gallery, find stitch, and add it. stitch, and add it. it'll ask for an api key. you can grab that from the stitch you can grab that from the stitch you can grab that from the stitch settings page. settings page. enter it into the antigravity ui and enter it into the antigravity ui and enter it into the antigravity ui and you're connected. your agent can now you're connected. your agent can now call tools like generate screen from call tools like generate screen from call tools like generate screen from text, edit screens, and list screens. text, edit screens, and list screens. second, chrome devtools mcp. this lets your coding agent inspect a this lets your coding agent inspect a this lets your coding agent inspect a live page in chrome, read the dom, check live page in chrome, read the dom, check live page in chrome, read the dom, check computed styles, look at the console. computed styles, look at the console. for this one, you just go to the github for this one, you just go to the github for this one, you just go to the github repo. you copy the antigravity config repo. you copy the antigravity config block. then, open the mcp settings in then, open the mcp settings in then, open the mcp settings in antigravity. switch to the raw config antigravity. switch to the raw config editor and paste it in. editor and paste it in. now, one little thing to watch for, the now, one little thing to watch for, the now, one little thing to watch for, the github config wraps the server in an mcp github config wraps the server in an mcp github config wraps the server in an mcp servers key. but, your config probably servers key. but, your config probably already has that parent. so, just paste already has that parent. so, just paste the inner server block. the inner server block. so, the workflow is one, design in so, the workflow is one, design in so, the workflow is one, design in stitch, prompt it with css concepts. stitch, prompt it with css concepts. two, inspect in chrome, use devtools mcp two, inspect in chrome, use devtools mcp two, inspect in chrome, use devtools mcp to validate the output. and three, to validate the output. and three, refine. feed what you learn back into refine. feed what you learn back into the next prompt. the next prompt. so, design, inspect, refine. one loop, so, design, inspect, refine. one loop, and the css knowledge is what makes each and the css knowledge is what makes each and the css knowledge is what makes each iteration better than the last. iteration better than the last. all right, we have the full setup, so all right, we have the full setup, so all right, we have the full setup, so let's let's go build something. let's let's go build something. and let's start with the layout. and a and let's start with the layout. and a quick note about this site. it's a site quick note about this site. it's a site about css features that uses those same about css features that uses those same about css features that uses those same css features. so, it's it's a meta site. css features. so, every card in this grid is a topic so, every card in this grid is a topic so, every card in this grid is a topic that we're going to cover today, built that we're going to cover today, built that we're going to cover today, built with the technique that it's describing. with the technique that it's describing. so, i prompted stitch to have this grid so, i prompted stitch to have this grid so, i prompted stitch to have this grid structure explicitly. structure explicitly. four columns, the scroll animations card four columns, the scroll animations card four columns, the scroll animations card spanning two columns and two rows as the spanning two columns and two rows as the spanning two columns and two rows as the hero. the customizable select in a tall single the customizable select in a tall single the customizable select in a tall single column cell, and the secondary features column cell, and the secondary features column cell, and the secondary features filling in around it. filling in around it. so, let's look at what came back. so, here is what stitch did. but, so, here is what stitch did. but, let's actually get this into the ide let's actually get this into the ide let's actually get this into the ide workflow. so, in stitch, i right-click the screen so, in stitch, i right-click the screen so, in stitch, i right-click the screen and i can grab this link. then i can go and i can grab this link. then i can go into anti-gravity and i paste in that into anti-gravity and i paste in that into anti-gravity and i paste in that link with a prompt to get the project link with a prompt to get the project link with a prompt to get the project info. anti-gravity talks to the stitch mcp and anti-gravity talks to the stitch mcp and anti-gravity talks to the stitch mcp and returns the project details, screen ids, returns the project details, screen ids, returns the project details, screen ids, and everything that i need. and everything that i need. now, i need to see this in a real now, i need to see this in a real now, i need to see this in a real browser, so the chrome devtools mcp can browser, so the chrome devtools mcp can browser, so the chrome devtools mcp can access it. so, i wrote a small cli tool for this, so, i wrote a small cli tool for this, so, i wrote a small cli tool for this, which you can access at npx at _davide which you can access at npx at _davide which you can access at npx at _davide /stitchmcp, /stitchmcp, /stitchmcp, and you can use the serve command. and and you can use the serve command. and this pulls the screen html down locally this pulls the screen html down locally this pulls the screen html down locally and serves it on, well, localhost. and serves it on, well, localhost. and now that it's running locally, the and now that it's running locally, the and now that it's running locally, the chrome devtools mcp can access the live chrome devtools mcp can access the live chrome devtools mcp can access the live dom. so, let's inspect what stitch actually so, let's inspect what stitch actually so, let's inspect what stitch actually generated. so, if we look at the grid container, so, if we look at the grid container, so, if we look at the grid container, stitch used grid template columns repeat stitch used grid template columns repeat stitch used grid template columns repeat 4 1fr. and the scroll animations card landed in and the scroll animations card landed in and the scroll animations card landed in colspan 2, rowspan 2, and that's the colspan 2, rowspan 2, and that's the colspan 2, rowspan 2, and that's the hero card. and the smaller feature cards all filled and the smaller feature cards all filled and the smaller feature cards all filled in around it. in around it. but, generating a static layouts is like but, generating a static layouts is like but