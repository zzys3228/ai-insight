kind: captions language: en [music] &gt;&gt; hi everyone. my name is thomas and today &gt;&gt; hi everyone. my name is thomas and today i'm here to introduce the html in canvas i'm here to introduce the html in canvas i'm here to introduce the html in canvas api. this api lets you draw dom content this api lets you draw dom content this api lets you draw dom content directly into a canvas or a webgl and directly into a canvas or a webgl and directly into a canvas or a webgl and webgpu texture while keeping the ui webgpu texture while keeping the ui webgpu texture while keeping the ui interactable, accessible, and hooked up interactable, accessible, and hooked up interactable, accessible, and hooked up to your favorite browser features. to your favorite browser features. all the demos that you see here are all the demos that you see here are all the demos that you see here are using the html that you know and using the html that you know and using the html that you know and hopefully love in ways that you never hopefully love in ways that you never hopefully love in ways that you never thought possible. thought possible. we'll get back to these, but first let we'll get back to these, but first let we'll get back to these, but first let me tell you a little bit more about this me tell you a little bit more about this me tell you a little bit more about this api. in this talk, we'll go over the basics in this talk, we'll go over the basics in this talk, we'll go over the basics of html in canvas and why each is great of html in canvas and why each is great of html in canvas and why each is great and even better together. and even better together. i'll then show you how you can start i'll then show you how you can start i'll then show you how you can start using the api today for your own project using the api today for your own project using the api today for your own project through some simple lines of code. through some simple lines of code. and finally, i'll show off some more of and finally, i'll show off some more of and finally, i'll show off some more of those cool demos and show you how you those cool demos and show you how you those cool demos and show you how you can apply the api directly. can apply the api directly. to really understand the api, let's to really understand the api, let's to really understand the api, let's first look closely at the dom and canvas first look closely at the dom and canvas first look closely at the dom and canvas to understand their relative strengths. to understand their relative strengths. i'll use dom and html somewhat i'll use dom and html somewhat i'll use dom and html somewhat interchangeably, but generally speaking interchangeably, but generally speaking interchangeably, but generally speaking html is the static source code you write html is the static source code you write html is the static source code you write while the dom is the live interactive while the dom is the live interactive while the dom is the live interactive model the browser builds from that code. model the browser builds from that code. as you'll see, this api changes both the as you'll see, this api changes both the as you'll see, this api changes both the static html on the page and updates the static html on the page and updates the static html on the page and updates the live dom structure to enable live dom structure to enable live dom structure to enable interactivity. interactivity. the dom is the staple of web ui and has the dom is the staple of web ui and has the dom is the staple of web ui and has incredible features hooked up through incredible features hooked up through incredible features hooked up through the browser. the browser. it offers great ui and text layout it offers great ui and text layout it offers great ui and text layout solutions out of the box that leverages solutions out of the box that leverages solutions out of the box that leverages semantically understood content to semantically understood content to semantically understood content to create rich and interactable interfaces. create rich and interactable interfaces. it enables users to do common operations it enables users to do common operations it enables users to do common operations across web pages including simple things across web pages including simple things across web pages including simple things we take for granted like selecting text we take for granted like selecting text we take for granted like selecting text to copy or right-clicking an image to to copy or right-clicking an image to to copy or right-clicking an image to save it. there are also a ton of browser there are also a ton of browser there are also a ton of browser integrations for features like integrations for features like integrations for features like accessibility, translate, find in page, accessibility, translate, find in page, accessibility, translate, find in page, reader mode, extensions, dark mode, reader mode, extensions, dark mode, reader mode, extensions, dark mode, browser zoom, and auto fill, which are browser zoom, and auto fill, which are browser zoom, and auto fill, which are all pivotal to how users interact with all pivotal to how users interact with all pivotal to how users interact with the web. canvas, along with webgl and webgpu on canvas, along with webgl and webgpu on canvas, along with webgl and webgpu on the other hand, give developers the other hand, give developers the other hand, give developers low-level access to enable highly low-level access to enable highly low-level access to enable highly advanced 2d and 3d graphics. advanced 2d and 3d graphics. the power of canvas is that it is a the power of canvas is that it is a the power of canvas is that it is a low-level system that can use various low-level system that can use various low-level system that can use various rendering stacks like webgpu or rendering stacks like webgpu or rendering stacks like webgpu or rendering frameworks to drive that grid rendering frameworks to drive that grid rendering frameworks to drive that grid of pixels. games and many web apps like google docs games and many web apps like google docs games and many web apps like google docs that have complex ui need this that have complex ui need this that have complex ui need this performant low-level access. performant low-level access. however, because the canvas is just a however, because the canvas is just a however, because the canvas is just a grid of pixels with some basic grid of pixels with some basic grid of pixels with some basic functionality, you'll need to handle a functionality, you'll need to handle a functionality, you'll need to handle a lot of logic like responsive text layout lot of logic like responsive text layout lot of logic like responsive text layout yourself or through a framework which yourself or through a framework which yourself or through a framework which adds to your bundle cost. adds to your bundle cost. additionally, all these features i additionally, all these features i additionally, all these features i prattled off earlier for the dom break prattled off earlier for the dom break prattled off earlier for the dom break completely when the ui is rendered into completely when the ui is rendered into completely when the ui is rendered into a canvas. until now, that is. the html in canvas api is the bridge the html in canvas api is the bridge the html in canvas api is the bridge that gives you the best of both worlds that gives you the best of both worlds that gives you the best of both worlds by letting you paint dom elements into a by letting you paint dom elements into a by letting you paint dom elements into a canvas or webgpu texture. canvas or webgpu texture. and the best part is that by putting the and the best part is that by putting the and the best part is that by putting the html inside the canvas tag and updating html inside the canvas tag and updating html inside the canvas tag and updating the transform, the content not only the transform, the content not only the transform, the content not only remains interactable, but all the remains interactable, but all the remains interactable, but all the browser features i mentioned stay hooked browser features i mentioned stay hooked browser features i mentioned stay hooked up, too. the api is available in chrome today, the api is available in chrome today, the api is available in chrome today, and we're actively engaged with other and we're actively engaged with other and we're actively engaged with other browsers on the open standard and hope browsers on the open standard and hope browsers on the open standard and hope to see it shipping across browsers in to see it shipping across browsers in to see it shipping across browsers in the future. using the api is done in three phases. setting up your canvas, setting up your canvas, setting up your canvas, rendering into the canvas or into a rendering into the canvas or into a rendering into the canvas or into a texture, texture, texture, and finally updating the transform of and finally updating the transform of and finally updating the transform of the element to tell the browser where it the element to tell the browser where it the element to tell the browser where it actually is. actually is. let's look at each of those steps let's look at each of those steps let's look at each of those steps through some code. through some code. the first thing to do is to set the the first thing to do is to set the the first thing to do is to set the layout subtree property on the canvas layout subtree property on the canvas layout subtree property on the canvas tag you're painting into. tag you're painting into. this causes the browser to be aware of this causes the browser to be aware of this causes the browser to be aware of the content in the canvas and sets it up the content in the canvas and sets it up the content in the canvas and sets it up to be hit tested and exposed to systems to be hit tested and exposed to systems to be hit tested and exposed to systems like accessibility and browser features. like accessibility and browser features. you can then just write the html code you can then just write the html code you can then just write the html code within the canvas tags such as adding a within the canvas tags such as adding a within the canvas tags such as adding a form element and an input form element and an input form element and an input and you can write this html directly as and you can write this html directly as and you can write this html directly as you're authoring or programmatically add you're authoring or programmatically add you're authoring or programmatically add it through javascript. it through javascript. putting the html inside the canvas tags putting the html inside the canvas tags putting the html inside the canvas tags is a prerequisite to rendering it, but is a prerequisite to rendering it, but is a prerequisite to rendering it, but it is not sufficient. rendering the it is not sufficient. rendering the element will require an explicit api element will require an explicit api element will require an explicit api call through a 2d context, webgl, or call through a 2d context, webgl, or call through a 2d context, webgl, or webgpu, which we'll take a look at next. webgpu, which we'll take a look at next. for a 2d context, the call is draw for a 2d context, the call is draw for a 2d context, the call is draw element image, which takes the dom element image, which takes the dom element image, which takes the dom element and an x and y location of where element and an x and y location of where element and an x and y location of where to draw the element into the canvas. to draw the element into the canvas. in this example, we're doing this in this example, we're doing this in this example, we're doing this through the on paint event, which through the on paint event, which through the on paint event, which triggers whenever the element is getting triggers whenever the element is getting triggers whenever the element is getting redrawn, including when the user or redrawn, including when the user or redrawn, including when the user or something like find in page highlights something like find in page highlights something like find in page highlights the text. this is the best way to make sure the ui this is the best way to make sure the ui this is the best way to make sure the ui is always updated, but you can do this is always updated, but you can do this is always updated, but you can do this wherever it makes sense for your wherever it makes sense for your wherever it makes sense for your application. application. the api method returns a transform that the api method returns a transform that the api method returns a transform that you can then apply to the css style you can then apply to the css style you can then apply to the css style transform property of the dom object to transform property of the dom object to transform property of the dom object to let the browser know where the element let the browser know where the element let the browser know where the element is being rendered. is being rendered. this is so that interactivity and this is so that interactivity and this is so that interactivity and browser functionality works browser functionality works browser functionality works automatically with no additional automatically with no additional automatically with no additional integrations. integrations. it's vital that you update this it's vital that you update this it's vital that you update this transform whenever you repaint the transform whenever you repaint the transform whenever you repaint the element. if you stop painting the element. if you stop painting the element, it's also critical to remove it element, it's also critical to remove it element, it's also critical to remove it from the canvas element, or else it'll from the canvas element, or else it'll from the canvas element, or else it'll keep being presented to the browser and keep being presented to the browser and keep being presented to the browser and accessibility systems as being part of accessibility systems as being part of accessibility systems as being part of the page. an important step is to set the width an important step is to set the width an important step is to set the width and height of the canvas to the right and height of the canvas to the right and height of the canvas to the right scale factor so that you avoid scale factor so that you avoid scale factor so that you avoid blurriness when rendering. blurriness when rendering. to do this, you just need to make sure to do this, you just need to make sure to do this, you just need to make sure to add these few lines of code to set a to add these few lines of code to set a to add these few lines of code to set a resize observer. resize observer. for webgl, you still need to set the fo