kind: captions language: en [music] &gt;&gt; hi, i'm scott, a developer relations &gt;&gt; hi, i'm scott, a developer relations &gt;&gt; hi, i'm scott, a developer relations engineer on web on android. and i'm engineer on web on android. and i'm sasha, a developer relations engineer on sasha, a developer relations engineer on sasha, a developer relations engineer on laptops and tablets. laptops and tablets. if you have an existing web app and you if you have an existing web app and you if you have an existing web app and you want to create an android application by want to create an android application by want to create an android application by leveraging your web investments, this leveraging your web investments, this leveraging your web investments, this video is for you. video is for you. we'll show you how to display your web we'll show you how to display your web we'll show you how to display your web content inside the android app and content inside the android app and content inside the android app and enhance it with powerful native android enhance it with powerful native android enhance it with powerful native android experiences. experiences. this lets you create a highly optimized this lets you create a highly optimized this lets you create a highly optimized app while reusing a lot of your existing app while reusing a lot of your existing app while reusing a lot of your existing web code. let's take a look at the website we let's take a look at the website we let's take a look at the website we built for this video. it's a simple built for this video. it's a simple note-taking app. note-taking app. and here is an android app built from and here is an android app built from and here is an android app built from it. a user sees a list of notes they've a user sees a list of notes they've a user sees a list of notes they've created. they can click on any of them and add they can click on any of them and add they can click on any of them and add some text. use keyboard shortcuts to make it bold, use keyboard shortcuts to make it bold, use keyboard shortcuts to make it bold, add a picture, add a picture, add a picture, download a note in pdf format, download a note in pdf format, download a note in pdf format, and even set a reminder that pops a and even set a reminder that pops a and even set a reminder that pops a native android push notification. native android push notification. so, how does this actually work behind so, how does this actually work behind so, how does this actually work behind the scenes? and how can you do the same the scenes? and how can you do the same for your web app? for your web app? we'll cover everything you need to know, we'll cover everything you need to know, we'll cover everything you need to know, webview fundamentals, how to implement webview fundamentals, how to implement webview fundamentals, how to implement tabs, tabs, tabs, how to set up two-way communication how to set up two-way communication how to set up two-way communication between webview and android apis, and between webview and android apis, and between webview and android apis, and finish with advanced webview apis, finish with advanced webview apis, finish with advanced webview apis, service workers, and offline support to service workers, and offline support to service workers, and offline support to create a polished app-like experience. create a polished app-like experience. sasha, why don't you start us off with sasha, why don't you start us off with sasha, why don't you start us off with the basics of webview? thanks, sasha. so, what exactly is a webview? think of it as a specialized web think of it as a specialized web think of it as a specialized web rendering engine based on the same rendering engine based on the same rendering engine based on the same chromium code that powers chrome that chromium code that powers chrome that chromium code that powers chrome that you can drop into your android app as a you can drop into your android app as a you can drop into your android app as a native component. native component. it's a seamless way to display web it's a seamless way to display web it's a seamless way to display web content without requiring your users to content without requiring your users to content without requiring your users to leave your app's context. leave your app's context. developers use webview for all kinds of developers use webview for all kinds of developers use webview for all kinds of things, like showing a terms of service things, like showing a terms of service things, like showing a terms of service or privacy policy page or embedding or privacy policy page or embedding or privacy policy page or embedding dynamic web content inside a native ui. dynamic web content inside a native ui. in our example app, we use a full-screen in our example app, we use a full-screen in our example app, we use a full-screen web view, which works in a mobile form web view, which works in a mobile form web view, which works in a mobile form factor and looks especially good on a factor and looks especially good on a factor and looks especially good on a large-screen device. large-screen device. keep in mind that a web view operates in keep in mind that a web view operates in keep in mind that a web view operates in its own isolated profile. this means it its own isolated profile. this means it doesn't share a cookie jar or local doesn't share a cookie jar or local doesn't share a cookie jar or local storage with chrome or any other browser storage with chrome or any other browser storage with chrome or any other browser on the device. on the device. this isolation is a deliberate design this isolation is a deliberate design this isolation is a deliberate design choice that ensures your app's data choice that ensures your app's data choice that ensures your app's data stays within its own sandbox. stays within its own sandbox. and since the app owns the web view, and since the app owns the web view, and since the app owns the web view, your native code is able to communicate your native code is able to communicate your native code is able to communicate with and modify the content of the web with and modify the content of the web with and modify the content of the web view. this is the key to enabling native this is the key to enabling native this is the key to enabling native android features in our web app. android features in our web app. let's take a look at the code. you can add a web view to your compose you can add a web view to your compose you can add a web view to your compose layout by wrapping it in an android layout by wrapping it in an android layout by wrapping it in an android view, and we're looking into how to make view, and we're looking into how to make view, and we're looking into how to make it even easier in the future. it even easier in the future. use the factory block to initialize the use the factory block to initialize the use the factory block to initialize the engine, change the web view settings, engine, change the web view settings, engine, change the web view settings, and load a url. and load a url. for our web app, the key settings we for our web app, the key settings we for our web app, the key settings we need are enabling javascript, which is need are enabling javascript, which is need are enabling javascript, which is disabled by default, enabling dom disabled by default, enabling dom disabled by default, enabling dom storage, which gives access to session storage, which gives access to session storage, which gives access to session storage and local storage, and modifying storage and local storage, and modifying storage and local storage, and modifying the user agent string so that our server the user agent string so that our server the user agent string so that our server gives us a desktop instead of mobile gives us a desktop instead of mobile gives us a desktop instead of mobile version when our app is used on a version when our app is used on a version when our app is used on a large-screen device. now that you know the basics, sasha will now that you know the basics, sasha will now that you know the basics, sasha will show how we've used some native ui show how we've used some native ui show how we've used some native ui elements to add tab support to our app. a big advantage of using web views is a big advantage of using web views is a big advantage of using web views is that we can mix them with native android that we can mix them with native android that we can mix them with native android elements to create even more powerful elements to create even more powerful elements to create even more powerful apps. let's look at our app. the main apps. the main part is the web view. it displays the part is the web view. it displays the web content and provides the main web content and provides the main web content and provides the main functionality. functionality. and on the top, you can see tabs. they are implemented using a native they are implemented using a native they are implemented using a native android structure. android structure. we've implemented this by wrapping the we've implemented this by wrapping the we've implemented this by wrapping the whole app in an android column, where whole app in an android column, where whole app in an android column, where the web view takes up the bottom space, the web view takes up the bottom space, the web view takes up the bottom space, and the top row sits on top. and the top row sits on top. the tab bar is another native android the tab bar is another native android the tab bar is another native android element, in our case, a lazy row. each element, in our case, a lazy row. each item in it is a material design card. it item in it is a material design card. it separates content from the background separates content from the background separates content from the background and provides clear visual structure. and provides clear visual structure. when the create new tab button is when the create new tab button is when the create new tab button is clicked, a separate web view is clicked, a separate web view is clicked, a separate web view is initialized and added to our state. initialized and added to our state. but how do we store these web views? and but how do we store these web views? and how do we switch between them without how do we switch between them without how do we switch between them without losing memory, javascript context, or losing memory, javascript context, or losing memory, javascript context, or scroll position? scroll position? to solve this, we do two things. first, we wrap each web view in a small first, we wrap each web view in a small first, we wrap each web view in a small custom data class called tab instance. custom data class called tab instance. we then store all tab instances in a we then store all tab instances in a we then store all tab instances in a stateful list. stateful list. this keeps stable references to the web this keeps stable references to the web this keeps stable references to the web views and prevents them from being views and prevents them from being views and prevents them from being garbage collected when they are off garbage collected when they are off garbage collected when they are off screen. second, when navigating between tabs, we second, when navigating between tabs, we second, when navigating between tabs, we don't destroy the non-selected web don't destroy the non-selected web don't destroy the non-selected web views. instead, we always keep them views. instead, we always keep them active and simply hide them. we stack active and simply hide them. we stack them inside a box and use modifier them inside a box and use modifier them inside a box and use modifier offset to move the hidden ones off offset to move the hidden ones off offset to move the hidden ones off screen. if we were to recreate the web view, the if we were to recreate the web view, the if we were to recreate the web view, the page would reload and lose its state. page would reload and lose its state. and that's how we've implemented tabs. now, scott will explain how to establish now, scott will explain how to establish now, scott will explain how to establish two-way communication between android two-way communication between android two-way communication between android and web. another benefit of web view is that it's another benefit of web view is that it's another benefit of web view is that it's not just a container for your web not just a container for your web not just a container for your web content. your native android code can actually your native android code can actually your native android code can actually talk directly to your web app's talk directly to your web app's talk directly to your web app's javascript engine, allowing you to do javascript engine, allowing you to do javascript engine, allowing you to do things like trigger js functions and things like trigger js functions and things like trigger js functions and manipulate the dom. manipulate the dom. and your web app's javascript code can and your web app's javascript code can and your web app's javascript code can call native functions, which lets your call native functions, which lets your call native functions, which lets your web app access android features. web app access android features. the two web view functions we're going the two web view functions we're going the two web view functions we're going to use are evaluate javascript, which to use are evaluate javascript, which to use are evaluate javascript, which handles native code to javascript handles native code to javascript handles native code to javascript communication, and add message listener, communication, and add message listener, communication, and add message listener, which adds javascript functions that can which adds javascript functions that can which adds javascript functions that can call native code. call native code. let's start with evaluate javascript and let's start with evaluate javascript and let's start with evaluate javascript and do some simple dom manipulation. do some simple dom manipulation. in our note-taking app, we have a in our note-taking app, we have a in our note-taking app, we have a feature that we want to remove from the feature that we want to remove from the feature that we want to remove from the app when it's running in a web view. app when it's running in a web view. specifically, this button. we can use web view's evaluate we can use web view's evaluate we can use web view's evaluate javascript command to execute any javascript command to execute any javascript command to execut