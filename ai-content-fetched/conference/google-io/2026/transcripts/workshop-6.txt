kind: captions language: en have you ever tried to build a complete have you ever tried to build a complete have you ever tried to build a complete rag system? rag system? well, rag is hard. in this video, i'll show you how to in this video, i'll show you how to in this video, i'll show you how to build powerful rag systems fast using build powerful rag systems fast using build powerful rag systems fast using gemini's file search api. gemini's file search api. plus, i'll show you some advanced data plus, i'll show you some advanced data plus, i'll show you some advanced data processing features if you hang around. processing features if you hang around. i'm mark mcdonald. i'm part of the i'm mark mcdonald. i'm part of the google deepmind developer experience google deepmind developer experience google deepmind developer experience team and i work mainly on gemini. team and i work mainly on gemini. this video doesn't require any coding this video doesn't require any coding this video doesn't require any coding experience, but it's made for developers experience, but it's made for developers experience, but it's made for developers who are building ai apps. so, having who are building ai apps. so, having some experience doing so, even by some experience doing so, even by some experience doing so, even by coding, will be useful. coding, will be useful. if you've ever built an app, you know if you've ever built an app, you know if you've ever built an app, you know that data is important. you have that data is important. you have content, something in your app that is content, something in your app that is content, something in your app that is important to you and your users. important to you and your users. maybe it's your personal to-do app. maybe it's an analysis engine that maybe it's an analysis engine that maybe it's an analysis engine that you've built. you've built. when you build an ai-powered app with when you build an ai-powered app with when you build an ai-powered app with something like the gemini api, you learn something like the gemini api, you learn something like the gemini api, you learn that ai isn't magical in and of itself. that ai isn't magical in and of itself. the real magic comes from combining your the real magic comes from combining your the real magic comes from combining your special sauce, like your to-do list, special sauce, like your to-do list, special sauce, like your to-do list, with focused ai prompts that work with focused ai prompts that work with focused ai prompts that work together. enabling your users to say something enabling your users to say something enabling your users to say something like, "what am i missing from my to-do like, "what am i missing from my to-do like, "what am i missing from my to-do list?" or "which file has the important list?" or "which file has the important list?" or "which file has the important thing that i forgot?" allows you to thing that i forgot?" allows you to thing that i forgot?" allows you to leverage your unique value proposition leverage your unique value proposition leverage your unique value proposition to create magical experiences. to create magical experiences. however, however, however, llms are finite. they have context llms are finite. they have context windows that impose an upper bound on windows that impose an upper bound on windows that impose an upper bound on the amount of data you can process in a the amount of data you can process in a the amount of data you can process in a single request. single request. and even if they were infinite, more and even if they were infinite, more and even if they were infinite, more tokens means slower responses and higher tokens means slower responses and higher tokens means slower responses and higher costs. having the ability to use a million having the ability to use a million having the ability to use a million tokens is pretty powerful, but sometimes tokens is pretty powerful, but sometimes tokens is pretty powerful, but sometimes less is more. less is more. rag, or retrieval augmented generation, rag, or retrieval augmented generation, rag, or retrieval augmented generation, is the usual way to scale beyond a is the usual way to scale beyond a is the usual way to scale beyond a model's context window. model's context window. this video won't explain it in depth, this video won't explain it in depth, this video won't explain it in depth, but essentially, you put all of your but essentially, you put all of your but essentially, you put all of your content in a data store, content in a data store, content in a data store, then when you need to answer a query, then when you need to answer a query, then when you need to answer a query, you selectively include relevant chunks you selectively include relevant chunks you selectively include relevant chunks into your prompt so that you're only into your prompt so that you're only into your prompt so that you're only working with relevant info. working with relevant info. this sounds simple, but the truth is this sounds simple, but the truth is this sounds simple, but the truth is that rag can be pretty complex. that rag can be pretty complex. there are different flavors of rag from there are different flavors of rag from there are different flavors of rag from regular naive rag to agentic rag, self regular naive rag to agentic rag, self regular naive rag to agentic rag, self rag, graph rag, iterative rag. rag, graph rag, iterative rag. at the architectural level, you already at the architectural level, you already at the architectural level, you already have a number of choices to think about have a number of choices to think about have a number of choices to think about before you even start designing your before you even start designing your before you even start designing your system. then, each component of the rag system then, each component of the rag system then, each component of the rag system needs to be considered, too. needs to be considered, too. which vector db to use? will it work which vector db to use? will it work with my own environment? how can i host with my own environment? how can i host it? then, you need to think about how to then, you need to think about how to then, you need to think about how to optimize your document processing, optimize your document processing, optimize your document processing, things like chunk size and overlapping things like chunk size and overlapping things like chunk size and overlapping windows, plus how you'll unpack complex windows, plus how you'll unpack complex windows, plus how you'll unpack complex file types like pdfs with tables and file types like pdfs with tables and file types like pdfs with tables and other unstructured data. other unstructured data. and then, your retrieval step has and then, your retrieval step has and then, your retrieval step has complexity, too. complexity, too. how do you map your user's question or how do you map your user's question or how do you map your user's question or prompt to search the data store? prompt to search the data store? do you set up query expansion or do you set up query expansion or do you set up query expansion or document re-ranking? document re-ranking? there's a lot to consider. and in a there's a lot to consider. and in a world where we want answers fast and ai world where we want answers fast and ai world where we want answers fast and ai promises rapid results, this can really promises rapid results, this can really promises rapid results, this can really slow down your prototyping. slow down your prototyping. this is where the gemini api's file this is where the gemini api's file this is where the gemini api's file search tool comes in. file search is a fully managed solution file search is a fully managed solution file search is a fully managed solution for rag that is built into the gemini for rag that is built into the gemini for rag that is built into the gemini api, api, api, abstracting most of the indexing and abstracting most of the indexing and abstracting most of the indexing and retrieval stages away so you can focus retrieval stages away so you can focus retrieval stages away so you can focus on your core logic. on your core logic. it's powered by the gemini embedding it's powered by the gemini embedding it's powered by the gemini embedding model, a state-of-the-art embedding model, a state-of-the-art embedding model, a state-of-the-art embedding model based on the same architecture as model based on the same architecture as model based on the same architecture as the powerful language model that you the powerful language model that you the powerful language model that you know and love. know and love. this ensures that the text isn't just this ensures that the text isn't just this ensures that the text isn't just matched on similarity, but on matched on similarity, but on matched on similarity, but on understanding, too. understanding, too. and it has automatic ocr built in so you and it has automatic ocr built in so you and it has automatic ocr built in so you can index multimodal content like pdfs, can index multimodal content like pdfs, can index multimodal content like pdfs, but with a purely text index. but with a purely text index. similar to a regular rag pipeline, there similar to a regular rag pipeline, there similar to a regular rag pipeline, there are two main phases. are two main phases. ingestion, that you do ahead of time to ingestion, that you do ahead of time to ingestion, that you do ahead of time to get your data indexed, and then search. get your data indexed, and then search. search is even easier here than a search is even easier here than a search is even easier here than a traditional rag setup, as it's just a traditional rag setup, as it's just a traditional rag setup, as it's just a tool that you tell the gemini api about. tool that you tell the gemini api about. then, the gemini model will use the tool then, the gemini model will use the tool then, the gemini model will use the tool to find the relevant documents, add them to find the relevant documents, add them to find the relevant documents, add them to the context, and produce whatever to the context, and produce whatever to the context, and produce whatever output is needed to fulfill the prompt. output is needed to fulfill the prompt. one of the powerful benefits of having one of the powerful benefits of having one of the powerful benefits of having file search provided as a tool is that file search provided as a tool is that file search provided as a tool is that the model is able to repeatedly call the the model is able to repeatedly call the the model is able to repeatedly call the tool, inspect the results, and refine or tool, inspect the results, and refine or tool, inspect the results, and refine or update the search queries that it is update the search queries that it is update the search queries that it is using to find the right information. using to find the right information. we call this agentic rag, and it's one we call this agentic rag, and it's one we call this agentic rag, and it's one of the most effective rag engines of the most effective rag engines of the most effective rag engines currently known. currently known. in this example, a user has sent a query in this example, a user has sent a query in this example, a user has sent a query asking how to apply for leave. asking how to apply for leave. within that single generate content call within that single generate content call within that single generate content call that your app makes, gemini will that your app makes, gemini will that your app makes, gemini will repeatedly make tool calls using the repeatedly make tool calls using the repeatedly make tool calls using the file search tool to answer the question. file search tool to answer the question. this is the agentic part. the user's query is ambiguous, as there the user's query is ambiguous, as there the user's query is ambiguous, as there are different types of leave that could are different types of leave that could are different types of leave that could apply. so, instead of stopping and asking the so, instead of stopping and asking the so, instead of stopping and asking the user, gemini starts with a query to find user, gemini starts with a query to find user, gemini starts with a query to find all of the different types of leave all of the different types of leave all of the different types of leave processes that might apply. processes that might apply. then, having read the results of those then, having read the results of those then, having read the results of those searches, it knows that the user needs searches, it knows that the user needs searches, it knows that the user needs to fill out specific forms, so it to fill out specific forms, so it to fill out specific forms, so it searches for them next. searches for them next. then, since the process requires then, since the process requires then, since the process requires approval, an additional search is done approval, an additional search is done approval, an additional search is done to figure out that step, too. to figure out that step, too. and finally, once the process is and finally, once the process is and finally, once the process is understood, well documented within the understood, well documented within the understood, well documented within the model's context window, it can formulate model's context window, it can formulate model's context window, it can formulate a response to the user detailing the a response to the user detailing the a response to the user detailing the different forms and processes required different forms and processes required different forms and processes required for whatever type of leave they are for whatever type of leave they are for whatever type of leave they are requesting. as you can see, the model can as you can see, the model can as you can see, the model can orchestrate a lot of processing as the orchestrate a lot of processing as the orchestrate a lot of processing as the result of just a single generate content result of just a single generate content result of just a single generate content call from your app. call from your app. so, it's still important that you have a so, it's still important that you have a so, it's still important that you have a human take a look at any output before human take a look at any output before human take a look at any output before you use it. the code for this is pretty the code for this is pretty the code for this is pretty straightforward, especially if you are straightforward, especially if you are straightforward, especially if you are familiar with the gemini sdk already. famili