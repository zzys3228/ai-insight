kind: captions language: en [music] &gt;&gt; hi, i'm alice and my colleagues and i &gt;&gt; hi, i'm alice and my colleagues and i &gt;&gt; hi, i'm alice and my colleagues and i will be sharing how to engineer will be sharing how to engineer will be sharing how to engineer performance applications. when we talk performance applications. when we talk about performance, we usually think of about performance, we usually think of about performance, we usually think of the visible metrics, a smooth ui and a the visible metrics, a smooth ui and a the visible metrics, a smooth ui and a fast startup. but memory is the silent fast startup. but memory is the silent foundation those metrics are built on. foundation those metrics are built on. if your memory usage is unoptimized, if your memory usage is unoptimized, if your memory usage is unoptimized, your app's performance and your user your app's performance and your user your app's performance and your user experience will be negatively impacted, experience will be negatively impacted, experience will be negatively impacted, too. think of your phone's physical memory think of your phone's physical memory think of your phone's physical memory capacity similar to this finite grid to capacity similar to this finite grid to capacity similar to this finite grid to illustrate a simplified version of illustrate a simplified version of illustrate a simplified version of memory page usage on a device. memory page usage on a device. every screen, high-res video buffering, every screen, high-res video buffering, every screen, high-res video buffering, or on-device ai model claims a chunk of or on-device ai model claims a chunk of or on-device ai model claims a chunk of these pages. as long as there are empty these pages. as long as there are empty pages, everything is smooth. but what pages, everything is smooth. but what happens if all the memory pages are used happens if all the memory pages are used happens if all the memory pages are used and there are no free pages left? and there are no free pages left? behind the scenes, the system scrambles behind the scenes, the system scrambles behind the scenes, the system scrambles to find more space, and that scramble to find more space, and that scramble to find more space, and that scramble can result in costly performance impacts can result in costly performance impacts can result in costly performance impacts such as ui jank, battery drain, anrs, such as ui jank, battery drain, anrs, such as ui jank, battery drain, anrs, and sudden app kills. and sudden app kills. for example, it could trigger frequent for example, it could trigger frequent for example, it could trigger frequent garbage collection to scan the heap for garbage collection to scan the heap for garbage collection to scan the heap for objects that are no longer being used. objects that are no longer being used. this occurring frequently can lead to this occurring frequently can lead to this occurring frequently can lead to visible stutters in the ui. visible stutters in the ui. when the free memory drops below a when the free memory drops below a when the free memory drops below a certain threshold, a kernel process certain threshold, a kernel process certain threshold, a kernel process reclaims and compresses memory pages. reclaims and compresses memory pages. this can take significant processing this can take significant processing this can take significant processing power on the cpu and result in the power on the cpu and result in the power on the cpu and result in the unpredictable app anrs and heavy battery unpredictable app anrs and heavy battery unpredictable app anrs and heavy battery drain. if free memory is critically low, the if free memory is critically low, the if free memory is critically low, the low memory killer steps in and starts low memory killer steps in and starts low memory killer steps in and starts terminating entire processes based on a terminating entire processes based on a terminating entire processes based on a strict priority hierarchy. strict priority hierarchy. this is the primary cause of cold this is the primary cause of cold this is the primary cause of cold starts, which forces users to have a starts, which forces users to have a starts, which forces users to have a prolonged loading screen while the app prolonged loading screen while the app prolonged loading screen while the app completely rebuilds its state. completely rebuilds its state. it can also cause highly disruptive it can also cause highly disruptive it can also cause highly disruptive sudden kills of visible processes, like sudden kills of visible processes, like sudden kills of visible processes, like a music player abruptly being shut down a music player abruptly being shut down a music player abruptly being shut down in the background while the os runs out in the background while the os runs out in the background while the os runs out of background processes to kill. of background processes to kill. not optimizing for memory consumption not optimizing for memory consumption not optimizing for memory consumption can also result in your app exceeding can also result in your app exceeding can also result in your app exceeding the bad behavior threshold for anrs or the bad behavior threshold for anrs or the bad behavior threshold for anrs or crashes, which may impact your app's crashes, which may impact your app's crashes, which may impact your app's discoverability on google play. discoverability on google play. finally, android is developing new finally, android is developing new finally, android is developing new methods of system-level optimizations to methods of system-level optimizations to methods of system-level optimizations to ensure stronger memory management and ensure stronger memory management and ensure stronger memory management and device stability across the ecosystem. device stability across the ecosystem. in this session, my colleagues and i are in this session, my colleagues and i are in this session, my colleagues and i are going to share three solutions for going to share three solutions for going to share three solutions for developers to optimize your memory, developers to optimize your memory, developers to optimize your memory, which will ultimately improve your app's which will ultimately improve your app's which will ultimately improve your app's user experience. user experience. i'll be sharing how to best configure i'll be sharing how to best configure i'll be sharing how to best configure your r8 rules. your r8 rules. and fung will explain some memory best and fung will explain some memory best and fung will explain some memory best practices to keep your memory usage practices to keep your memory usage practices to keep your memory usage lean. finally, i'll be sharing observability finally, i'll be sharing observability finally, i'll be sharing observability tooling so that you can catch memory tooling so that you can catch memory tooling so that you can catch memory issues in the field. issues in the field. by the end of this talk, you'll have a by the end of this talk, you'll have a by the end of this talk, you'll have a proactive roadmap to ensure your app proactive roadmap to ensure your app proactive roadmap to ensure your app continues to be performant. one of the most impactful changes you one of the most impactful changes you one of the most impactful changes you can make to improve your app's can make to improve your app's can make to improve your app's performance is to enable the r8 performance is to enable the r8 performance is to enable the r8 optimizer. while r8 is widely known for shrinking while r8 is widely known for shrinking while r8 is widely known for shrinking apk sizes, apk sizes, apk sizes, it also helps reduce memory usage. it also helps reduce memory usage. when less code is loaded from apk into when less code is loaded from apk into when less code is loaded from apk into ram, the app consumes less memory. ram, the app consumes less memory. r8 removes unused code and resources r8 removes unused code and resources r8 removes unused code and resources from your app, reducing the memory from your app, reducing the memory from your app, reducing the memory needed to load your app. needed to load your app. you want to do this because if you you want to do this because if you you want to do this because if you don't, unoptimized heap memory usage don't, unoptimized heap memory usage don't, unoptimized heap memory usage triggers frequent garbage collection, triggers frequent garbage collection, triggers frequent garbage collection, causing ui jank. causing ui jank. r8 minifies classes, methods, and fields r8 minifies classes, methods, and fields r8 minifies classes, methods, and fields into shorter names, reducing memory into shorter names, reducing memory into shorter names, reducing memory usage. this is important because stack usage. this is important because stack memory issues contribute to a complex, memory issues contribute to a complex, memory issues contribute to a complex, less efficient runtime. less efficient runtime. r8 performs optimizations such as method r8 performs optimizations such as method r8 performs optimizations such as method inlining to replace a method called with inlining to replace a method called with inlining to replace a method called with the actual code and merges multiple the actual code and merges multiple the actual code and merges multiple classes, reducing the heap size and classes, reducing the heap size and classes, reducing the heap size and stack memory. stack memory. for your users, this means faster for your users, this means faster for your users, this means faster startup time, improved rendering, and startup time, improved rendering, and startup time, improved rendering, and fewer anrs. now that we have seen the benefits, now that we have seen the benefits, now that we have seen the benefits, let's enable r8 in your code base. let's enable r8 in your code base. in your app's build.gradle file, set is in your app's build.gradle file, set is in your app's build.gradle file, set is shrinking resources is equal to true. shrinking resources is equal to true. this enables r8 to remove unused this enables r8 to remove unused this enables r8 to remove unused resources and code. resources and code. is minifyenabled is equal to true allows is minifyenabled is equal to true allows is minifyenabled is equal to true allows r8 to optimize the code, remove the r8 to optimize the code, remove the r8 to optimize the code, remove the unused classes, and rename the classes unused classes, and rename the classes unused classes, and rename the classes to reduce the code size. to reduce the code size. and finally, define the default proguard and finally, define the default proguard and finally, define the default proguard file, which contains the optimization file, which contains the optimization file, which contains the optimization rules. a key thing to make sure is to use the a key thing to make sure is to use the a key thing to make sure is to use the correct default proguard file. correct default proguard file. this legacy file prevents r8 from this legacy file prevents r8 from this legacy file prevents r8 from applying optimizations to your app. applying optimizations to your app. note that from android gradle plugin 9, note that from android gradle plugin 9, note that from android gradle plugin 9, the legacy file proguard-android.txt the legacy file proguard-android.txt the legacy file proguard-android.txt is no longer supported. is no longer supported. so, replace the incorrect default file so, replace the incorrect default file so, replace the incorrect default file with proguard-android-optimize.txt with proguard-android-optimize.txt with proguard-android-optimize.txt that is bundled with platform tools. that is bundled with platform tools. this seemingly small configuration this seemingly small configuration this seemingly small configuration update can significantly improve several update can significantly improve several update can significantly improve several performance metrics. performance metrics. if your gradle properties file includes if your gradle properties file includes if your gradle properties file includes a flag to disable r8 full mode, remove a flag to disable r8 full mode, remove a flag to disable r8 full mode, remove the flag to unlock r8's full potential. the flag to unlock r8's full potential. make sure to thoroughly test your app make sure to thoroughly test your app make sure to thoroughly test your app after these changes as it enables after these changes as it enables after these changes as it enables powerful optimization. powerful optimization. making these changes can have a major making these changes can have a major making these changes can have a major impact on several performance metrics. impact on several performance metrics. take the example of monzo, take the example of monzo, take the example of monzo, a uk digital bank with 15 million a uk digital bank with 15 million a uk digital bank with 15 million customers and growing. customers and growing. as the app scaled, the engineering team as the app scaled, the engineering team as the app scaled, the engineering team identified app startup time as a identified app startup time as a identified app startup time as a critical area for improvement, but critical area for improvement, but critical area for improvement, but worried it would require significant worried it would require significant worried it would require significant changes to their code base. they sought changes to their code base. they sought a solution that would enhance the a solution that would enhance the a solution that would enhance the performance without the overhead of a performance without the overhead of a performance without the overhead of a complete rewrite. by fully enabling r8 complete rewrite. by fully enabling r8 optimization, monzo achieved a massive optimization, monzo achieved a massive optimization, monzo achieved a massive 35% reduction in their application not 35% reduction in their application not 35% reduction in their application not responding rate. responding rate. and you know what? the team also saw a and you know what? the team also saw a 30% improvement in slow cold startup 30% improvement in slow cold startup 30% improvement in slow cold startup time and reduced their overall app size time and reduced their overall app size time and reduced their overall app size by 9%. to summarize, enable r8 with shrink to summarize, enable r8 with shrink to summarize, enable r8 with shrink resources, correct proguard file and in resources, correct proguard file and in resources, correct proguard file and in full mode to get the maximum full mod