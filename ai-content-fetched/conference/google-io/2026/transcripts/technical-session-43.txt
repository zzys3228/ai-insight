kind: captions language: en hi everyone and welcome to building hi everyone and welcome to building hi everyone and welcome to building custom design systems with jetpack custom design systems with jetpack custom design systems with jetpack compose's new styles api. compose's new styles api. my name is rebecca and with me today on my name is rebecca and with me today on my name is rebecca and with me today on stage we have stage we have stage we have &gt;&gt; megan &gt;&gt; megan &gt;&gt; megan &gt;&gt; and chuck. &gt;&gt; and chuck. &gt;&gt; we've seen how android is evolving to &gt;&gt; we've seen how android is evolving to &gt;&gt; we've seen how android is evolving to support different device form factors support different device form factors support different device form factors and how apps are increasingly wanting to and how apps are increasingly wanting to and how apps are increasingly wanting to stand out from the crowd with their own stand out from the crowd with their own stand out from the crowd with their own custom design system definitions. custom design system definitions. we understand that every app is unique. many apps use material from a full many apps use material from a full many apps use material from a full material based design system to a full material based design system to a full material based design system to a full custom design system. and if you fall custom design system. and if you fall somewhere in between the spectrum, we somewhere in between the spectrum, we somewhere in between the spectrum, we are building a framework that works are building a framework that works are building a framework that works across this. across this. jetsnack, one of our very first examples jetsnack, one of our very first examples jetsnack, one of our very first examples we released in compose, has always been we released in compose, has always been we released in compose, has always been a great showcase of building something a great showcase of building something a great showcase of building something custom in compose that leverages custom in compose that leverages custom in compose that leverages material components under the hood. material components under the hood. but its design system has always had its but its design system has always had its but its design system has always had its own unique definition own unique definition own unique definition in that it has used a very broad usage in that it has used a very broad usage in that it has used a very broad usage of gradients throughout it. of gradients throughout it. the component designs have typically the component designs have typically the component designs have typically been slightly different from the been slightly different from the been slightly different from the components that material design offers components that material design offers components that material design offers out of the box. out of the box. it's used gradients in various different it's used gradients in various different it's used gradients in various different ways, such as this filter chip that has ways, such as this filter chip that has ways, such as this filter chip that has a gradient border for the normal state a gradient border for the normal state a gradient border for the normal state and the pressed state is a is a solid and the pressed state is a is a solid and the pressed state is a is a solid gradient. separately, when looking at material separately, when looking at material separately, when looking at material components, they come built in with components, they come built in with components, they come built in with really great defaults. and this is why really great defaults. and this is why we love using material. it handles all we love using material. it handles all of these complexities for you, such as of these complexities for you, such as of these complexities for you, such as accessibility standards, right-to-left accessibility standards, right-to-left accessibility standards, right-to-left positioning, various different positioning, various different positioning, various different interaction states, and components interaction states, and components interaction states, and components generally look great out of the box. generally look great out of the box. but as we've seen, our designer has but as we've seen, our designer has but as we've seen, our designer has given us something that's a little bit given us something that's a little bit given us something that's a little bit more custom. more custom. so how would you typically go about so how would you typically go about so how would you typically go about this? for this filter chip example, you've for this filter chip example, you've for this filter chip example, you've likely tried importing the material likely tried importing the material likely tried importing the material filter chip, and then immediately tried filter chip, and then immediately tried filter chip, and then immediately tried to set a background color that's a to set a background color that's a to set a background color that's a gradient when it's selected. gradient when it's selected. and at this point, you probably see that and at this point, you probably see that and at this point, you probably see that the api was only designed with a single the api was only designed with a single the api was only designed with a single color in mind, and it doesn't support color in mind, and it doesn't support color in mind, and it doesn't support the brush type. the brush type. you may at this point probably do one of you may at this point probably do one of you may at this point probably do one of a few things. in this case, i set the a few things. in this case, i set the colors to transparent, and then surround colors to transparent, and then surround colors to transparent, and then surround the chip in a box, and use the chip in a box, and use the chip in a box, and use modifier.background on the box to try modifier.background on the box to try modifier.background on the box to try and actually set the gradient instead. and actually set the gradient instead. then moving the filter chip to be within then moving the filter chip to be within then moving the filter chip to be within this box. things are looking okay with this until things are looking okay with this until things are looking okay with this until we actually try and run them. we can see we actually try and run them. we can see that we also need to apply the shape to that we also need to apply the shape to that we also need to apply the shape to the container, too. the container, too. not only that, but we also need to build not only that, but we also need to build not only that, but we also need to build in our own logic to handle pressed and in our own logic to handle pressed and in our own logic to handle pressed and selected states now. selected states now. so, at this point, you might have also so, at this point, you might have also so, at this point, you might have also decided it was time to fork the material decided it was time to fork the material decided it was time to fork the material source or create your own version of source or create your own version of source or create your own version of these components. these components. in jetsnack, we've created some of our in jetsnack, we've created some of our in jetsnack, we've created some of our own components. however, this means that own components. however, this means that our app is losing all of these benefits our app is losing all of these benefits our app is losing all of these benefits of material. of material. so, we've been working on something new so, we've been working on something new so, we've been working on something new that we want to preview with you today that we want to preview with you today that we want to preview with you today called styles. called styles. styles are a new mechanism in jetpack styles are a new mechanism in jetpack styles are a new mechanism in jetpack compose for customizing elements. compose for customizing elements. they're designed to separate form from they're designed to separate form from they're designed to separate form from function and unlock easy state-based function and unlock easy state-based function and unlock easy state-based styling along with some added styling along with some added styling along with some added performance benefits. performance benefits. the styles api has been released in the styles api has been released in the styles api has been released in version 1.12 of compose foundation as version 1.12 of compose foundation as version 1.12 of compose foundation as experimental experimental experimental with materials adoption in the apis with materials adoption in the apis with materials adoption in the apis coming soon. coming soon. you'll see this is a big shift in how you'll see this is a big shift in how you'll see this is a big shift in how you interact with components today. you interact with components today. we will show you how styles helps we will show you how styles helps we will show you how styles helps customize your components in an customize your components in an customize your components in an easy-to-use way through a refreshed easy-to-use way through a refreshed easy-to-use way through a refreshed version of jetsnack. version of jetsnack. still using gradients, just a tad more still using gradients, just a tad more still using gradients, just a tad more subtle. to get i'll give it over to subtle. to get i'll give it over to megan to tell you more. &gt;&gt; thank you, rebecca. so, let's hop into &gt;&gt; thank you, rebecca. so, let's hop into it. like we said, we're giving jetsnack it. like we said, we're giving jetsnack a makeover using the style api, but you a makeover using the style api, but you a makeover using the style api, but you might all be asking, "what's a style?" might all be asking, "what's a style?" might all be asking, "what's a style?" a style is an interface that defines the a style is an interface that defines the a style is an interface that defines the appearance of a component. appearance of a component. a style is a specialized modifier that a style is a specialized modifier that a style is a specialized modifier that automatically gives performance and automatically gives performance and automatically gives performance and reusability benefits. reusability benefits. and i really want to stress that styles and i really want to stress that styles and i really want to stress that styles are not here to replace modifiers, but are not here to replace modifiers, but are not here to replace modifiers, but instead here to replace parameters that instead here to replace parameters that instead here to replace parameters that are related to visual properties. let me are related to visual properties. let me show you what i mean. show you what i mean. as we mentioned in this talk, we were as we mentioned in this talk, we were as we mentioned in this talk, we were going to be working with jetsnack, which going to be working with jetsnack, which going to be working with jetsnack, which uses a custom implementation of button uses a custom implementation of button uses a custom implementation of button to achieve the gradient effect. to achieve the gradient effect. and as you can see, this definition of and as you can see, this definition of and as you can see, this definition of button has many different styling button has many different styling button has many different styling parameters, disabled state, shape, parameters, disabled state, shape, parameters, disabled state, shape, border, etc. but with styles, we can border, etc. but with styles, we can replace all of those lines with replace all of those lines with replace all of those lines with a simple style parameter, which provides a simple style parameter, which provides a simple style parameter, which provides a standardized set of properties that a standardized set of properties that a standardized set of properties that can be set on any component. can be set on any component. so, now you know what a style is, okay. so, now you know what a style is, let's see how you would use a style. let's see how you would use a style. there are two use cases for adding there are two use cases for adding there are two use cases for adding styles to your components. one is styles to your components. one is creating a custom component and making creating a custom component and making creating a custom component and making it styleable, and the other is using a it styleable, and the other is using a it styleable, and the other is using a composable that has a style parameter. composable that has a style parameter. so, let's look at how to make a custom so, let's look at how to make a custom so, let's look at how to make a custom component styleable. under the hood, jetsnack's button is under the hood, jetsnack's button is under the hood, jetsnack's button is implemented as a row that is clickable. implemented as a row that is clickable. and in order to make the button and in order to make the button and in order to make the button styleable, you can add a styleable styleable, you can add a styleable styleable, you can add a styleable modifier. and then, you would add a style and then, you would add a style and then, you would add a style parameter to your method signature, so parameter to your method signature, so parameter to your method signature, so that implementations of the custom that implementations of the custom that implementations of the custom button can use the style parameter to button can use the style parameter to button can use the style parameter to customize their implementation of the customize their implementation of the customize their implementation of the button. and it's always recommended to provide a and it's always recommended to provide a and it's always recommended to provide a style style style style parameter on design system style parameter on design system style parameter on design system components, in the same way that you components, in the same way that you components, in the same way that you always supply a modifier. always supply a modifier. then, you'll pass in style state and the then, you'll pass in style state and the then, you'll pass in style state and the style parameter into the style able style parameter into the style able style parameter into the style able modifier and we're not going to get into modifier and we're not going to get into modifier and we're not going to get into style state right now, but we will get style state right now, but we will get style state right now, but we will get into it later in this talk. and now our into it later in this talk. and now our custom button is ready to use styles. custom button is ready to use styles. the other use case that you would use is the other use case