kind: captions language: en [music] &gt;&gt; hi, i'm maryam, a product manager for &gt;&gt; hi, i'm maryam, a product manager for &gt;&gt; hi, i'm maryam, a product manager for flutter. today, we're going to share seven tools today, we're going to share seven tools today, we're going to share seven tools and techniques you can use right now to and techniques you can use right now to and techniques you can use right now to make your flutter code more readable, make your flutter code more readable, make your flutter code more readable, scalable, and testable. scalable, and testable. i'm joined by eric and john, who will i'm joined by eric and john, who will i'm joined by eric and john, who will help me demonstrate these techniques to help me demonstrate these techniques to help me demonstrate these techniques to build a checkout experience in our dash build a checkout experience in our dash build a checkout experience in our dash shop app, an exclusive online store for shop app, an exclusive online store for shop app, an exclusive online store for dash gear, stuffies, keyboards, you name dash gear, stuffies, keyboards, you name dash gear, stuffies, keyboards, you name it. these techniques will help you build these techniques will help you build these techniques will help you build your flutter app with a scalable and your flutter app with a scalable and your flutter app with a scalable and maintainable app architecture, so it can maintainable app architecture, so it can maintainable app architecture, so it can go the distance. go the distance. and we'll also share some tools that you and we'll also share some tools that you and we'll also share some tools that you can use to add features to your app, and can use to add features to your app, and can use to add features to your app, and some cool ways that ai can use them to some cool ways that ai can use them to some cool ways that ai can use them to assist you. all right, let's dive into the code. the first technique is all about the first technique is all about the first technique is all about building a great ui. design using building a great ui. design using constraints and not fixed sizes. constraints and not fixed sizes. flutter makes it possible for us to have flutter makes it possible for us to have flutter makes it possible for us to have the dash store available on any device the dash store available on any device the dash store available on any device and any screen with a single code base. and any screen with a single code base. but with ease comes responsibility, as a but with ease comes responsibility, as a but with ease comes responsibility, as a wise bird once told me. wise bird once told me. it's our job as developers to ensure the it's our job as developers to ensure the it's our job as developers to ensure the ui is visually responsive while keeping ui is visually responsive while keeping ui is visually responsive while keeping the code readable and error-free. the code readable and error-free. designing using constraints makes your designing using constraints makes your designing using constraints makes your widgets flexible and more reusable than widgets flexible and more reusable than widgets flexible and more reusable than if they were using fixed sizes. if they were using fixed sizes. that way, you can easily reuse them for that way, you can easily reuse them for that way, you can easily reuse them for mobile, desktop, or the web. mobile, desktop, or the web. essentially, that boils down to three essentially, that boils down to three essentially, that boils down to three steps: abstract, measure, and branch. steps: abstract, measure, and branch. the ui currently uses flutter's flex the ui currently uses flutter's flex the ui currently uses flutter's flex layout with widgets like row, column, layout with widgets like row, column, layout with widgets like row, column, and flex to help make sure the widgets and flex to help make sure the widgets and flex to help make sure the widgets resize based on the size of the resize based on the size of the resize based on the size of the constraints. constraints. but when the screen gets too small to but when the screen gets too small to but when the screen gets too small to display everything or so large that display everything or so large that display everything or so large that there's wasted screen space, there's wasted screen space, there's wasted screen space, you need to swap out the layout with an you need to swap out the layout with an you need to swap out the layout with an entirely different one appropriate for entirely different one appropriate for entirely different one appropriate for the screen size. the screen size. so, the first step is to identify the ui so, the first step is to identify the ui so, the first step is to identify the ui widgets that need to change and abstract widgets that need to change and abstract widgets that need to change and abstract the underlying data they share. the underlying data they share. in this case, the cart screen layout in this case, the cart screen layout in this case, the cart screen layout needs to change based on the screen needs to change based on the screen needs to change based on the screen size. we'll pass the data it needs to render we'll pass the data it needs to render we'll pass the data it needs to render in via the constructor parameter. in via the constructor parameter. the widget shouldn't care about where the widget shouldn't care about where the widget shouldn't care about where the data comes from, just that it needs the data comes from, just that it needs the data comes from, just that it needs to display it. to display it. next, let's measure the size of your next, let's measure the size of your next, let's measure the size of your display area. display area. use media query size of for the entire use media query size of for the entire use media query size of for the entire app window or layout builder for the app window or layout builder for the app window or layout builder for the localized constraints. localized constraints. since this is a smaller widget that we since this is a smaller widget that we since this is a smaller widget that we want to reuse, we'll use layout builder. want to reuse, we'll use layout builder. finally, we can read the incoming finally, we can read the incoming finally, we can read the incoming constraints that layout builder gives us constraints that layout builder gives us constraints that layout builder gives us and return different widgets based on and return different widgets based on and return different widgets based on the available size. the available size. to learn more, check out the layout docs to learn more, check out the layout docs to learn more, check out the layout docs linked in the description. linked in the description. if our ui layout is broken or the app if our ui layout is broken or the app if our ui layout is broken or the app freezes, we could fiddle with the height freezes, we could fiddle with the height freezes, we could fiddle with the height of the container, hot reload, fiddle of the container, hot reload, fiddle of the container, hot reload, fiddle with the height again, hot reload again, with the height again, hot reload again, with the height again, hot reload again, or we could save some time and skip all or we could save some time and skip all or we could save some time and skip all of the guesswork by using dev tools to of the guesswork by using dev tools to of the guesswork by using dev tools to provide visibility into the app, such as provide visibility into the app, such as provide visibility into the app, such as the exact constraints that the parent the exact constraints that the parent the exact constraints that the parent widget is enforcing. widget is enforcing. our second technique is use dev tools. keeping accessibility in mind, let's keeping accessibility in mind, let's keeping accessibility in mind, let's make the images and the text in the make the images and the text in the make the images and the text in the checkout cart bigger so that they are checkout cart bigger so that they are checkout cart bigger so that they are more visible. more visible. that should be pretty easy. i'll open up anti-gravity and enter a i'll open up anti-gravity and enter a i'll open up anti-gravity and enter a prompt with the widget i want to change. prompt with the widget i want to change. so, anti-gravity made the change. now, i'll tell it to reload. and we hit a classic issue, and we hit a classic issue, and we hit a classic issue, the dreaded layout overflow error. the dreaded layout overflow error. to help us understand what's going on, to help us understand what's going on, to help us understand what's going on, let's use dev tools to see which widget let's use dev tools to see which widget let's use dev tools to see which widget is causing the error. is causing the error. the flutter dev tools inspector the flutter dev tools inspector the flutter dev tools inspector highlights these errors so we can see highlights these errors so we can see highlights these errors so we can see which widget is causing the issue. which widget is causing the issue. this row widget, this row widget, this row widget, which is overflowing. which is overflowing. we can dig into the details ourselves or we can dig into the details ourselves or we can dig into the details ourselves or we can use the dart and flutter mcp we can use the dart and flutter mcp we can use the dart and flutter mcp server to help us fix the issue. server to help us fix the issue. the mcp server makes flutter's developer the mcp server makes flutter's developer the mcp server makes flutter's developer tools available to ai assistance. tools available to ai assistance. to install it in antigravity, we'll open to install it in antigravity, we'll open to install it in antigravity, we'll open up the agent panel and select mcp up the agent panel and select mcp up the agent panel and select mcp servers under the additional options servers under the additional options servers under the additional options drop-down menu and search for dart. drop-down menu and search for dart. once installed, antigravity displays the once installed, antigravity displays the once installed, antigravity displays the available tools. available tools. when we run our app, our agent can when we run our app, our agent can when we run our app, our agent can connect to it directly. connect to it directly. and once it's connected, the agent can and once it's connected, the agent can and once it's connected, the agent can read the layout error message using the read the layout error message using the read the layout error message using the get runtime errors tool. get runtime errors tool. layout errors contain a lot of details layout errors contain a lot of details layout errors contain a lot of details that gemini is really good at that gemini is really good at that gemini is really good at understanding. understanding. the agent knows how to use the hot the agent knows how to use the hot the agent knows how to use the hot reload and get widget tree tools to help reload and get widget tree tools to help reload and get widget tree tools to help make sure that it fixed the layout make sure that it fixed the layout make sure that it fixed the layout error. mixing business logic and ui within a mixing business logic and ui within a mixing business logic and ui within a widget creates massive, unreadable, widget creates massive, unreadable, widget creates massive, unreadable, deeply nested build methods. deeply nested build methods. by separating your business logic from by separating your business logic from by separating your business logic from your ui, your widgets become easier to your ui, your widgets become easier to your ui, your widgets become easier to reuse, modify, and test. reuse, modify, and test. so, technique three is building reusable so, technique three is building reusable so, technique three is building reusable widgets. as mentioned before, a widget should as mentioned before, a widget should as mentioned before, a widget should receive all the configuration it needs receive all the configuration it needs receive all the configuration it needs to render as arguments. to render as arguments. it shouldn't have to modify or format it shouldn't have to modify or format it shouldn't have to modify or format any data. its only jobs are to render any data. its only jobs are to render data and listen for user events. data and listen for user events. as a rule of thumb, if your widget is as a rule of thumb, if your widget is as a rule of thumb, if your widget is importing low-level libraries, making importing low-level libraries, making importing low-level libraries, making remote requests, or interacting with a remote requests, or interacting with a remote requests, or interacting with a database, it's probably not reusable. database, it's probably not reusable. in the dash store app, when a user in the dash store app, when a user in the dash store app, when a user presses purchase, the widget doesn't presses purchase, the widget doesn't presses purchase, the widget doesn't turn around and call the payments api turn around and call the payments api turn around and call the payments api directly. so, in order to keep the widget reusable so, in order to keep the widget reusable so, in order to keep the widget reusable and only focused on displaying the ui, and only focused on displaying the ui, and only focused on displaying the ui, that logic is deferred to a different that logic is deferred to a different that logic is deferred to a different object using a callback that is passed object using a callback that is passed object using a callback that is passed in as a parameter to the widget's in as a parameter to the widget's in as a parameter to the widget's constructor. constructor. when your widgets are reusable, you can when your widgets are reusable, you can when your widgets are reusable, you can leverage tools like widget previews. leverage tools like widget previews. widget previews is a developer tool that widget previews is a developer tool that widget previews is a developer tool that complements hot reload, enabling you to complements hot reload, enabling you to complements hot reload, enabling you to experiment with ui variations and states experiment with ui variations and states experiment with ui variations and states without running the full application. without running the full application. rather than running an app, navigating rather than running an app, navigating rather than running an app, navigating to a specific screen, and getting the to a specific screen, and getting the to a specific screen, and getting the app into a specific state just to test app into a specific state just to test app into a specific state just to