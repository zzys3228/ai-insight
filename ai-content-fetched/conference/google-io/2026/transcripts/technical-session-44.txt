kind: captions language: en [music] &gt;&gt; so, over the past several years, google &gt;&gt; so, over the past several years, google &gt;&gt; so, over the past several years, google has been driving an incredible pace of has been driving an incredible pace of has been driving an incredible pace of innovation in ai. and when you look innovation in ai. and when you look across this timeline, you will see an across this timeline, you will see an across this timeline, you will see an evolution of models and modalities all evolution of models and modalities all evolution of models and modalities all the way from gemini to gemma, leo, the way from gemini to gemma, leo, the way from gemini to gemma, leo, imagen, and omni that was announced just imagen, and omni that was announced just imagen, and omni that was announced just yesterday. so, what's incredible about this so, what's incredible about this so, what's incredible about this timeline is that it's not just the timeline is that it's not just the timeline is that it's not just the cadence of cadence of cadence of but the breakthrough capabilities that but the breakthrough capabilities that but the breakthrough capabilities that are being unlocked with every launch. are being unlocked with every launch. things like multimodal understanding, things like multimodal understanding, things like multimodal understanding, reasoning, and agentic workflows. reasoning, and agentic workflows. so, today we want to go one layer deeper so, today we want to go one layer deeper so, today we want to go one layer deeper and talk to you about what powers these and talk to you about what powers these and talk to you about what powers these models. and not just from a silicon models. and not just from a silicon standpoint, but from a full software standpoint, but from a full software standpoint, but from a full software stack standpoint. and talk to you about stack standpoint. and talk to you about some of the tools that you can use as some of the tools that you can use as some of the tools that you can use as you build, train, fine-tune, serve you build, train, fine-tune, serve you build, train, fine-tune, serve state-of-the-art models on google cloud state-of-the-art models on google cloud state-of-the-art models on google cloud tpus. okay, awesome. so, as you know, there's okay, awesome. so, as you know, there's roughly four stages to building a large roughly four stages to building a large roughly four stages to building a large language model. first, you would design language model. first, you would design your neural network, and to do that your neural network, and to do that your neural network, and to do that you'd use a framework like jax. then you you'd use a framework like jax. then you have pre-training, post-training, and have pre-training, post-training, and have pre-training, post-training, and inference. in pre-training, you're inference. in pre-training, you're teaching the model basically to predict teaching the model basically to predict teaching the model basically to predict the next token. in post-training, you're the next token. in post-training, you're teaching the model to be useful. you teaching the model to be useful. you could do something like supervised could do something like supervised could do something like supervised fine-tuning to teach it to follow fine-tuning to teach it to follow fine-tuning to teach it to follow instructions. and then you could do instructions. and then you could do something like reinforcement learning to something like reinforcement learning to something like reinforcement learning to teach it to reason. and then after that, teach it to reason. and then after that, you need to serve the model to users or you need to serve the model to users or you need to serve the model to users or do inference. and what's really cool is do inference. and what's really cool is if you look at this chart, you can look if you look at this chart, you can look if you look at this chart, you can look at roughly where the intelligence is at roughly where the intelligence is at roughly where the intelligence is coming from. and what's really cool coming from. and what's really cool recently is you'll see that a lot of the recently is you'll see that a lot of the recently is you'll see that a lot of the intelligence is actually coming from intelligence is actually coming from intelligence is actually coming from inference. and if you think about why that is, it's and if you think about why that is, it's and if you think about why that is, it's because now we have thinking models that because now we have thinking models that because now we have thinking models that are consuming lots of tokens as they are consuming lots of tokens as they are consuming lots of tokens as they reason over your problems. and so, reason over your problems. and so, what's cool is we have these two what's cool is we have these two what's cool is we have these two distinct phases of compute. we have distinct phases of compute. we have training compute and we have inference training compute and we have inference training compute and we have inference compute. and the needs are a little bit compute. and the needs are a little bit different, and we've designed our different, and we've designed our different, and we've designed our hardware stack, our tpu stack, with that hardware stack, our tpu stack, with that hardware stack, our tpu stack, with that in mind. yeah, so as josh mentioned, um yeah, so as josh mentioned, um yeah, so as josh mentioned, um &gt;&gt; [clears throat] &gt;&gt; [clears throat] &gt;&gt; [clears throat] &gt;&gt; as you can see here, there is a clear &gt;&gt; as you can see here, there is a clear &gt;&gt; as you can see here, there is a clear separation from like an architectural separation from like an architectural separation from like an architectural standpoint between systems that standpoint between systems that standpoint between systems that specialize in training workloads and specialize in training workloads and specialize in training workloads and those that specialize in inference those that specialize in inference those that specialize in inference workloads. so, tpu v8 is our latest workloads. so, tpu v8 is our latest generation of tpus that we announced at generation of tpus that we announced at generation of tpus that we announced at cloud next. and uh tpu 8t is highly cloud next. and uh tpu 8t is highly optimized for large-scale training optimized for large-scale training optimized for large-scale training workloads. so, the focus here are things workloads. so, the focus here are things like maximizing your throughput and like maximizing your throughput and like maximizing your throughput and scaling efficiency. and tpu 8i is great scaling efficiency. and tpu 8i is great for inference workloads. so, the focus for inference workloads. so, the focus is our things like low latency and cost is our things like low latency and cost is our things like low latency and cost efficiency. so, we're moving away from one so, we're moving away from one so, we're moving away from one general-purpose system into hardware general-purpose system into hardware general-purpose system into hardware that is specialized for the needs of that is specialized for the needs of that is specialized for the needs of different types of workloads. but, different types of workloads. but, hardware is just one part of the puzzle. hardware is just one part of the puzzle. so, let's talk about some of the so, let's talk about some of the so, let's talk about some of the software tools that we mentioned software tools that we mentioned software tools that we mentioned earlier. so, this talk will take okay, awesome. so, this talk will take you through a bunch of software that you you through a bunch of software that you you through a bunch of software that you can try at home. we're going to start can try at home. we're going to start with inference cuz we just talked about with inference cuz we just talked about with inference cuz we just talked about how important that is for thinking how important that is for thinking how important that is for thinking models. um a really cool thing to say models. um a really cool thing to say about inference, pre-training a model about inference, pre-training a model about inference, pre-training a model from scratch takes a lot of compute, but from scratch takes a lot of compute, but from scratch takes a lot of compute, but if you're working on inference and also if you're working on inference and also if you're working on inference and also post-training, that's absolutely post-training, that's absolutely post-training, that's absolutely something you can do with the free tpus something you can do with the free tpus something you can do with the free tpus that are available in kaggle or on that are available in kaggle or on that are available in kaggle or on colab. so, we'll start with a demo of colab. so, we'll start with a demo of serving gemma 4 using vllm on tpus, and serving gemma 4 using vllm on tpus, and serving gemma 4 using vllm on tpus, and then we'll move into another demo of uh then we'll move into another demo of uh then we'll move into another demo of uh doing some post-training with a doing some post-training with a doing some post-training with a different version of gemma using tunix, different version of gemma using tunix, different version of gemma using tunix, which is our lightweight post-training which is our lightweight post-training which is our lightweight post-training framework. then quickly we'll go through framework. then quickly we'll go through maxtext, which has high-performance um maxtext, which has high-performance um maxtext, which has high-performance um reference implementations of all sorts reference implementations of all sorts reference implementations of all sorts of large language models implemented in of large language models implemented in of large language models implemented in jax, and these will scale out of the box jax, and these will scale out of the box jax, and these will scale out of the box to thousands of chips, which are super to thousands of chips, which are super to thousands of chips, which are super cool. and then we'll talk about cool. and then we'll talk about frameworks for actually building models. frameworks for actually building models. so, jax, which is used to build uh so, jax, which is used to build uh so, jax, which is used to build uh gemini and gemma, and then we'll talk gemini and gemma, and then we'll talk gemini and gemma, and then we'll talk about torch tpu, which is really cool. about torch tpu, which is really cool. it's a um software we're working on to it's a um software we're working on to it's a um software we're working on to make it really easy for you to run your make it really easy for you to run your make it really easy for you to run your pytorch code on tpus without without pytorch code on tpus without without pytorch code on tpus without without code changes. code changes. and to get started, um, rob awesome. and to get started, um, rob will will will give a demo on vllm. i will give a demo on vllm. i will do a quick intro, and then you have to do a quick intro, and then you have to do a quick intro, and then you have to hold for like 5 minutes for the demo. hold for like 5 minutes for the demo. &gt;&gt; oh, sorry, rob. i jumped i jumped the &gt;&gt; oh, sorry, rob. i jumped i jumped the gun. no, it's no, it's no, it's let's start with inference. so, let's start with inference. so, uh, uh, uh, llm serving is no longer just about llm serving is no longer just about llm serving is no longer just about compute. in fact, at scale, some of the compute. in fact, at scale, some of the harder problems are usually memory harder problems are usually memory harder problems are usually memory management, scheduling, and hardware management, scheduling, and hardware management, scheduling, and hardware utilization, and also being able to utilization, and also being able to utilization, and also being able to abstract away all of these complexities. abstract away all of these complexities. this is where vllm comes in. let's talk this is where vllm comes in. let's talk about some of these challenges, and then about some of these challenges, and then about some of these challenges, and then we'll uh talk about how vllm helps you we'll uh talk about how vllm helps you we'll uh talk about how vllm helps you address those. address those. so, as context length grow and so, as context length grow and so, as context length grow and concurrency increases, kv cache concurrency increases, kv cache concurrency increases, kv cache management becomes one of the biggest management becomes one of the biggest management becomes one of the biggest bottlenecks in inference. bottlenecks in inference. so, vllm uses paged attention. it virtualizes kv cache into fixed-size it virtualizes kv cache into fixed-size it virtualizes kv cache into fixed-size blocks. so, this tremendously helps blocks. so, this tremendously helps improve utilization. but, this is just a improve utilization. but, this is just a foundation, and there are several foundation, and there are several foundation, and there are several optimizations on top as well. optimizations on top as well. for example, prefix caching is ideal for for example, prefix caching is ideal for for example, prefix caching is ideal for agentic workloads and conversational agentic workloads and conversational agentic workloads and conversational workloads. the idea is for several uh workloads. the idea is for several uh requests to have one common prefix. so, requests to have one common prefix. so, instead of the system having to instead of the system having to instead of the system having to repeatedly calculate kv cache, it can repeatedly calculate kv cache, it can repeatedly calculate kv cache, it can simply use whatever was previously simply use whatever was previously simply use whatever was previously calculated. so, let's talk a little bit about so, let's talk a little bit about so, let's talk a little bit about scheduling now. scheduling now. inference traffic is highly irregular. you are constantly dealing with you are constantly dealing with you are constantly dealing with different prompt lengths, generation different prompt lengths, generation different prompt lengths, generation times, arrival patterns, and so on. times, arrival patterns, and so on. vllm uses vllm uses vllm uses uh continuous batching. so, instead of uh continuous batching. so, instead of using static request-level batches, your using static request-level batches, your using static request-level batches, your system dynamically schedules token-level system dynamically schedules token-level system dynamically schedules token-level work at runtime. work at runtime.